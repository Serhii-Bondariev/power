// back/server/routes/cart.js
import express from 'express';

const router = express.Router();

// Напишемо тимчасовий маршрут, щоб уникнути помилки
router.get('/', (req, res) => {
  res.status(200).json({ message: 'Cart route' });
});

export default router;
// Після створення маршруту /api/cart, ви можете виконати запит GET на цей маршрут, щоб перевірити, чи працює маршрут. Якщо ви отримаєте відповідь з повідомленням Cart route, це означає, що маршрут працює правильно.
// Якщо ви отримаєте помилку, переконайтеся, що ви правильно налаштували маршрут /api/cart. Якщо ви використовуєте Postman, переконайтеся, що ви виконуєте запит GET на правильний URL-адресу, наприклад, http://localhost:5000/api/cart.
// Якщо ви використовуєте Postman, переконайтеся, що ви виконуєте запит GET на правильний URL-адресу, наприклад, http://localhost:5000/api/cart.
// Якщо ви використовуєте Postman, переконайтеся, що ви виконуєте запит GET на правильний URL-адресу, наприклад, http://localhost:5000/api/cart.
// Якщо ви використовуєте Postman, переконайтеся, що ви виконуєте запит GET на правильний URL-адресу, наприклад, http://localhost:5000/api/cart.